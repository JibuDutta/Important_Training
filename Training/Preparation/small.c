#include<stdio.h>
int small(int x, int y , int z)
{
	int c = 0;

	while( x && y && z)
	{
		f(x & (1 << 31))
		{
			if( y & (1 << 31))
			{
				if( z & (1 << 31))

				{
					x
				
				}
			}
		}
		x--; 	y--;	z--;
		c++;
	}
	return c;
}

int main()
{
	int a,b,c,d = 0;
	printf("Enter the three no:");
	scanf("%d%d%d",&a,&b,&c);

	d = small(a,b,c);
	
	printf("smallest is %d\n",d);

	return 0;
}
